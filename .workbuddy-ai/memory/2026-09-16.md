# 2026-09-16 — P2 item 8, layer 2 (`proxy/`, `sync/`, `eventBus.ts`)

Continues from `2026-09-15.md` (layer 1 + the vitest resolution fix, committed as `675c52a`).

## What moved

28 files: `src/proxy/` (19), `src/sync/` (8), `src/eventBus.ts` (1).

**They had to move together, and the plan's ordering was wrong.** `plan/03` listed `eventBus.ts`
*after* `proxy/` and `sync/`, but `eventBus.ts` imports `@/proxy/logEmitter`, `@/proxy/webhookServer`
(types) and `@/sync/statusTracker` (a value), while `proxy/**` imports `@/eventBus` for
`bus.emitTyped`. Moving any one alone would have left the engine importing *out* of its own package.
Measured the coupling before starting rather than trusting the doc.

## Rewrite: 189 statements across 95 files, in five passes

Layer 1's two passes were not enough. Two new classes of rewrite, both silent-failure traps:

- **Pass 4 — `@bifurc/engine/*` self-imports inside the package must become relative.** After layer
  1 the moved files already contained 23 such imports. They *appear* to work, but resolve through
  the `exports` map to `dist/` at runtime while the rest of the engine loads from source — two live
  copies of module state, exactly the silent-singleton failure `bundle: false` exists to prevent.
- **Pass 5 — `vi.mock()`.** An import-shaped regex (`from "…"` / `import(` / `require(`) does not
  match `vi.mock("@/sync/autoSync", …)`; it is a plain call. The first four passes rewrote 177
  specifiers and still left **12 stale mock specifiers across 7 files**. A mock whose specifier no
  longer matches its subject is worse than no mock — the real module loads and the test silently
  stops being hermetic. Guarded to skip `tests/renderer/**`, whose `@/lib/*`, `@/hooks/*` and
  `@/components/*` mocks belong to the renderer's own alias space.

## Two grep false positives, resolved differently

- `eventBus.ts`'s doc comment quoted `BrowserWindow.getAllWindows()` → **reworded** (keep the
  information, drop the token), matching what layer 1 did for the store comments.
- `proxy/service-discovery.ts` holds a literal Windows path ending in `powershell.exe`, which the
  unanchored pattern `shell\.` matched → **refined the criterion to `\bshell\.`** rather than
  mangling working code. The word-boundary form is the correct semantic (Electron's `shell` API is
  always a standalone identifier) and still catches `shell.openExternal(url)` in
  `src/ipc/handlers/clientHandlers.ts`. Plan doc updated.

## Verification

- `build:packages` → `dist/proxy/`, `dist/sync/`, `dist/eventBus.js` emitted; **invariant 1 holds**
  (`dist/proxy/server.js` does `require("../store/config")` / `require("./constants")` — module graph
  preserved, no bundling). `eventBus.js` only requires `./sync/statusTracker`, because its two
  `proxy/` imports were `import type` and correctly erased.
- `typecheck` / `build:main` clean. `dist/startup.js` requires `@bifurc/engine/proxy/server` etc. as
  bare specifiers — resolved via the workspace symlink + `exports` map.
- **Full suite 1615/1616 over 69 files** — identical to baseline; the 1 failure is the documented
  `soap.execute` sandbox caveat.
- **Coverage 47.99 / 31.99 / 36.30 / 50.38** (thresholds 46/31/34/48). Denominator unchanged at
  11,443 statements across both layers.
- **Per-file: 26 of 27 layer-2 files identical on all four metrics including raw covered/total
  counts; 0 regressions, 0 missing, 0 stale `src/` entries.**
- The one mover, `sync/gitOps.ts` (78.8% → 82.4%), has **identical totals**. Line-level lcov data
  identified it exactly: line 114 (`git checkout HEAD -- <path>`) succeeded 4× before / 1× after,
  and the `ls-files` fallback at lines 118–128 ran 2× / 5×, including the inner checkout at lines
  125–127 which was **never executed before and ran 3× after**. Real-git nondeterminism in the temp
  workspace, not the move — and a previously-dead fallback path is now covered.

## New finding — packaging does not follow the package split

`build.files` is `["dist/**/*","package.json"]`. electron-builder **does** add production deps'
`node_modules`, but it does not dereference the `node_modules/@bifurc/*` workspace symlinks — the
last real asar (`release/win-unpacked/resources/app.asar`, built 2026-09-10) has **no
`node_modules/@bifurc`**. Meanwhile the current `dist/` requires `@bifurc/protocol`
(`dist/commands/registry.js`, since item 7) and `@bifurc/engine` (20+ files, since items 7–8).

So a **packaged** build cannot resolve either specifier. Dev mode and the whole test suite are
unaffected (the symlink resolves locally). Stated precisely: the 09-10 asar predates both
dependencies, so it cannot demonstrate the failure directly — it confirms only what gets copied.
Confirming end-to-end needs one `electron-builder --dir` run.

**Not fixed here** — `plan/12` already owns this ("Change point 1 — at P6"), and P6 replaces the
mechanism with an `extraResources` binary anyway. Recorded in `plan/12` with the evidence, in
`plan/13` as an open item, in `plan/03`, and in `MEMORY.md`.

## Docs updated

`TESTING.md` (§4.1 numbers, §4.2 area table + footnotes, §4.8 split into layer 1 / layer 2 with the
verification tables, §5 exclude-entry warning, all `src/proxy|sync` paths), `plan/03` (status block,
target tree corrected — `events/bus.ts` never existed — still-to-do list, honest status, acceptance
grep), `plan/13` (git mv, alias resolution, dependency split, gate), `plan/README.md` (P2 row),
`plan/12` (measured packaging note), and both skills.

## Layer 3 — `applications/`, `companion/`, `commands/`

8 files (`applications/` 4, `companion/` 2, `commands/` 2), 48 statements rewritten across 26 files
(45 imports + 3 `vi.mock`). `ws` joined the engine as its third runtime dependency
(`companion/companionServer.ts`), exactly as predicted when `companion/` was still in `src/`.

**Measured clean before starting**, and it showed: the only `@/` import in the three directories was
`@/companion/allowedActions` — internal to the layer. Every other dependency was either a Node
builtin or an already-moved `@bifurc/engine/*`. That is why layer 3 needed no special handling and
came back **7 of 7 exact** on all four coverage metrics (7, not 8 — the 8th file was absent from the
pre-move report because it was excluded), the cleanest layer yet.

**Verification:** suite 1615/1616 over 69 files (identical to baseline); coverage
**48.00 / 31.99 / 36.30 / 50.39** (thresholds 46/31/34/48); 0 regressions, 0 missing, 0 stale `src/`
entries. Both acceptance greps ZERO. `dist/companion/companionServer.js` uses relative requires plus
`ws`, so `bundle: false` still holds.

### Found: a `coverage.exclude` entry that was wrong all along

`src/applications/types.ts` was listed under "Type-only modules". It is not type-only — it exports
`DEFAULT_DEBUG_PORTS`, `RUN_CONFIG_TYPE_LABELS` and `RUN_CONFIG_TYPE_ICONS` as real runtime values.
The exclusion was made on the strength of the filename and had been hiding live, fully-covered code
from the report the whole time.

Rather than carry a wrong exclusion to its new path, the entry was **removed**. The file now reports
3/3 lines at 100%, which is why the denominator grew by exactly 3 statements (11,443 → 11,446) and
the numerator by exactly 3 — a scope change that is entirely explained and entirely an improvement.

This is the mirror image of the layer-1/2 problem (a stale exclude that silently stops matching).
The lesson recorded in the skill: never relocate an exclude entry without checking the file really
is type-only (`grep -nE '^export (const|function|class|let|var|enum|default)' <file>`).

### Tooling: the rewriter is now part of the skill

Written three times by hand, so it is now `scripts/rewrite-imports.js` inside
`bifurc-engine-layer-move`, with all five passes and the `tests/renderer/**` guard baked in:

```bash
node ~/.workbuddy-ai/skills/bifurc-engine-layer-move/scripts/rewrite-imports.js \
  --dirs applications,companion,commands [--files eventBus] [--dry]
```

## Layer 4 — `startup.ts` + `shutdown.ts` (extraction complete)

2 files, 17 statements rewritten across 6 files. **2 of 2 identical on all four metrics, 0
regressions, 0 missing, 0 stale entries.** The cleanest layer to reason about: neither file had a
single `@/` import, and every dependency was a Node builtin or an already-moved `@bifurc/engine/*`.

**`src/` is now empty of engine code** — only `ipc/` (the Electron registration layer P2 replaces),
`main.ts` and `preload.ts` remain, all deliberately. 52 files moved across the four layers.

### The headline dipped, and the cause was not this layer

Coverage went 48.00% → 47.98% (3 statements). I diffed every file between run8 and run9: the **only**
file that changed is `sync/gitOps.ts`, with totals unchanged at 85. It has now been observed at
**68 → 71 → 71 → 68** covered statements across the four runs — up in layer 2, back down in layer 4.

That oscillation, in both directions, is the definitive proof that it is real-git temp-workspace
state and not the move. Line-level lcov data agrees: the difference is a `git checkout HEAD -- <path>`
attempt succeeding or falling through to the `ls-files` recovery branch. **No file that actually
moved changed its coverage in any layer.**

Recorded in the skill as a rule: when a real-I/O file's number moves, check the **totals**. Identical
denominators prove the code was untouched; a changed denominator means the move altered the code.

## `createEngine()` — item 8's last deliverable

`createEngine({ dataDir, onPreflightWarning? })` → `{ start(), stop(), status(), registry, bus }`,
implemented in `packages/engine/src/index.ts`. `start()` sets the data root, runs the advisory
preflight, bootstraps workspaces, binds the proxy and companion sockets, and waits for readiness.
`stop()` delegates to the memoised `shutdownEngine()`.

Proven by `tests/integration/engineSmoke.integration.test.ts` — plain Node, no Electron: real temp
data root, real bind, a real HTTP request through the proxy, real teardown with the port released,
idempotent `start()`/`stop()`, and `start()` after `stop()` **rejecting**.

**Three things the smoke test caught that a unit test could not:**

1. **`start()` resolved before the engine was listening.** `startServer()` and
   `startCompanionServer()` are fire-and-forget — both hand off to an async `listen()` and return
   immediately — so `isRunning()` is still false on the next line. Fixed with a bounded readiness
   wait; otherwise the single promise a caller awaits would mean nothing.
2. **`registry.list()` is empty, by design.** `createEngine()` registers no commands; the ~112 are
   registered by the *consumer* (the shell today, the RPC client at P6). The test now asserts
   registry *identity*, not population.
3. **The proxy routes on `Host`.** Requesting `127.0.0.1` gets a 400 "Not a localhost domain"; the
   proxy's own UI is served under `localhost`. The test sends `Host: localhost:<port>` while
   connecting to `127.0.0.1`.

### Found: the data root is not authoritative on Windows

`workspaceFs.dataRoot()` and `appSettings.settingsPath()` check `%LOCALAPPDATA%` **before**
consulting the data root, so on Windows `setDataRoot()` is unreachable and the engine writes to the
user's real `%LOCALAPPDATA%\Bifurc`. Verified against the built package: after
`setDataRoot("C:/tmp/probe")`, `paths.dataDir()` returns `C:\tmp\probe` while
`workspaceFs.dataRoot()` returns `C:\Users\…\AppData\Local\Bifurc\data`. On macOS and Linux the
branch does not exist, so `dataDir` works as documented.

**Not fixed, deliberately.** The branch's value is exactly `platformDefaultDataDir()`, so it is the
store's own platform default rather than an accident; making `setDataRoot()` win would change where
an existing Windows install looks for its workspaces — a user-visible change, out of bounds for P2.
`e2e/fixtures/electronApp.ts` already overrides `LOCALAPPDATA` for this reason, and the new smoke
test does the same. Recorded in `plan/03` with the decision needed before P8 (CLI) or P9 (Docker)
rely on `--data-dir`.

### Result

- Suite **1616/1617 over 70 files** (the 1 failure is the documented `soap.execute` caveat).
- Coverage **48.21 / 32.21 / 36.52 / 50.59** (was 46.14 / 31.30 / 34.34 / 48.39), denominator
  11,129 → 11,489.
- `packages/engine/src/index.ts` now reports **90.7% statements / 94.6% lines**, and there are
  **no engine files left at 0%** — the barrel that was deliberately not excluded is covered, exactly
  as intended.
- Acceptance criterion "engine starts from a bare Node script with `--data-dir`" is met on macOS and
  Linux, and on Windows with the `LOCALAPPDATA` override.

**P2 work item 8 is complete.** Remaining P2 items are the ones deliberately left open on product
grounds: the git `dialog.showErrorBox` not routed through `preflight()`, and `importExport:*` outside
the registry until P3.

## ⚠️ INCIDENT — local git object store destroyed, and recovered

### What happened

While verifying the `@bifurc/protocol` dependency fix I ran `git stash` and then a vitest command that
exceeded the tool's 120 s timeout. The SIGTERM landed while git was running an automatic repack. The
interrupted gc removed `.git/objects/pack/*.pack` and never completed a replacement, so the object
database was left empty — **0 loose objects, 0 pack files**. The packs were not unlinked: Windows
diverted them to the **I: drive Recycle Bin**.

A second symptom made it look worse than it was: `refs/` had been pruned away (it had held only
`refs/stash`, which the pop removed). Git requires `refs/` to exist to treat a directory as a valid
git dir, so it silently walked *up* to the empty repo at `I:/workspace/Bifurc-Worskspace/.git` and
reported "your current branch 'main' does not have any commits yet".

### Recovery, in order

1. **Located the packs** — `find /i -name '*.pack'` found five in `I:/$RECYCLE.BIN/S-1-5-.../`. Their
   mtimes matched Bifurc's activity (Sep 14 23:05 → Sep 16 10:29).
2. **Preserved them first**, before anything else could overwrite them:
   `I:/workspace/Bifurc-Worskspace/_recovered-packs/` (11 MB, outside the repo so never staged).
3. **Recreated `.git/refs/{heads,tags}`** — this alone made git recognise `Bifurc/.git` again.
4. **Re-indexed the packs** into `.git/objects/pack/` as `rec1..rec5.{pack,idx}` via `git index-pack`.
   This restored **all of P0/P1/P2 and layers 1–3** — `476d871` through `ac65173`.
5. **Restored `refs/heads/standalone-engine`** with `git update-ref` from the reflog (it had been a
   loose ref inside the pruned `refs/` tree).
6. **Rebuilt `.git/index`** — `rm .git/index && git reset`. The stale index (written 18:08) referenced
   blobs from the lost pack, which is what made `git status` fail with "unable to read <sha>". Worth
   noting: I first blamed HEAD's tree, but none of the missing blobs were in it — the fault was
   entirely the index.
7. **`git fetch origin --tags`** restored `origin/master` and the tags.
8. **Preserved the reflogs** to `_recovered-packs/RECOVERED-reflog-*.txt` — the only surviving record
   of the two lost commits and their messages.

### What was lost, and what was not

- **Lost:** two commits — `9be9492` (layer 4) and `5008f85` (createEngine) — plus the uncommitted
  dependency fix. Their objects were the newest, and sat in the pack the interrupted gc was writing.
- **Not lost:** the entire working tree. Every file was intact on disk the whole time, so no *content*
  was ever at risk. The two commit messages survive in the saved reflogs.
- **Re-landed** as a single commit `33bd5c9`, whose message records both original messages, the
  incident and the residual damage. Splitting it back into two is a trivial `git reset --soft`.

### Residual damage

14 blobs are still missing. They are **not** reachable from `33bd5c9`'s tree — `git status` and
`git diff HEAD` both read the full tree successfully, so the tip is sound — and they appear to belong
to objects left over from the `filter-branch` rewrites recorded in the reflog. Some older historical
commits may not be fully checkable-out. Bounded; investigate only if it matters.

### Lessons

- **Never `git stash` in this repo.** A temp commit or `git worktree` achieves the same thing without
  creating a `refs/stash` whose removal prunes the `refs/` tree around it.
- **Never let a long command get SIGTERM'd while git is mid-operation.** Background long test suites.
- **The tells are diagnostic:** git resolving to the *parent* directory means `refs/` is gone;
  `git status` failing with "unable to read <sha>" means the *index* is stale, not the tree.
- **Preserve before you repair.** Copying the Recycle Bin packs out first is what made this
  recoverable at all.

## Next

P2 item 8 is complete. Next is P3 (`importExport:*` into the registry — also where
`js-yaml`/`archiver`/`unzipper` move to the engine), then P4/P5 (RPC transport), then P6.




